buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.2'
    }
}

@TaskAction
void myTaskAction() {
    // Your task action logic
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
